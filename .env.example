# Optional: production URL for sitemap.xml and robots.txt.
# In dev, defaults to https://mineperformance.com if unset.
# NEXT_PUBLIC_SITE_URL=https://yoursite.com

# Do not commit real API keys, secrets, or tokens. Use env vars and .env.local (gitignored).
